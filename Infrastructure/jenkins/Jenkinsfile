pipeline {
    agent {
            kubernetes {
        label 'jenkins-agent'
        yamlFile 'Infrastructure/jenkins/runner.yaml'
        defaultContainer 'builder'
        }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}